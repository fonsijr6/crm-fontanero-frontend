
# Frontend (Angular)

Crea el proyecto con Angular CLI y coloca aquí el código.

```bash
npm i -g @angular/cli
ng new frontend --routing --style=scss
```
Luego mueve el contenido dentro de esta carpeta si has creado fuera.
